const CreateBlogForm = ({handleSubmit}) => {

    return (
        <div>
            <h2>create new</h2>
            <form onSubmit={handleSubmit}>
                
            </form>
        </div>
    )
}

export default CreateBlogForm